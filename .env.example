# Server
PORT=8000
HOST=0.0.0.0
NODE_ENV=production
LOG_LEVEL=info

# Override WebSocket host returned in TwiML (optional).
# If not set, derived from the incoming request host header.
# Example: SERVER_HOST=your-ec2-public-dns.compute.amazonaws.com
SERVER_HOST=
